{
  "buildCommand": "cd src && npm install -g pnpm && pnpm install && pnpm generate",
  "outputDirectory": "src/.output/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
